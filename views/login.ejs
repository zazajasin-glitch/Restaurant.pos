<%- include("layout", {
  title: "تسجيل دخول",
  user: null,
  body: `
  <div class="row justify-content-center">
    <div class="col-12 col-md-5 col-lg-4">
      <div class="card glass p-3">
        <h4 class="mb-3 fw-bold">تسجيل دخول</h4>
        ${error ? `<div class="alert alert-danger">${error}</div>` : ``}
        <form method="post" action="/login" class="d-grid gap-2">
          <input class="form-control form-control-lg" name="username" placeholder="اسم المستخدم" required>
          <input class="form-control form-control-lg" name="password" type="password" placeholder="كلمة المرور" required>
          <button class="btn btn-primary btn-lg">دخول</button>
          <div class="text-secondary small">admin / admin123</div>
        </form>
      </div>
    </div>
  </div>
`}) %>
